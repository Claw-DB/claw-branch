[advisories]
ignore = [
  "RUSTSEC-2023-0071", # rsa timing side-channel in transitive deps; no fix available yet
  "RUSTSEC-2024-0384", # instant unmaintained via backoff transitive chain
  "RUSTSEC-2025-0012", # backoff unmaintained via claw-guard dependency
]
